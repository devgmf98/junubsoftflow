# The API lives on Railway; the front end is served by Netlify, so this must be
# absolute. Netlify reads it at build time and Vite inlines it into the bundle.
VITE_API_URL=https://junubsoftflow.up.railway.app/api
