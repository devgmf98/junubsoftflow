# Leave unset for local development: the Vite proxy forwards /api to the API on
# port 4000, which keeps the session cookie first-party.
# Set it to the deployed API's absolute base when the front end is hosted apart
# from the API, e.g. https://your-api.up.railway.app/api
# VITE_API_URL=
