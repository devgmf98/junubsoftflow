{
  "name": "junubsoftflow",
  "version": "1.0.0",
  "private": true,
  "description": "SoftFlow - buy software online. React frontend + Express/MySQL backend.",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "setup": "npm --prefix backend run setup",
    "migrate": "npm --prefix backend run migrate",
    "seed": "npm --prefix backend run seed",
    "dev:api": "npm --prefix backend run dev",
    "dev:web": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend install --include=dev --no-audit --no-fund && npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "postinstall": "cd backend && npm install --omit=dev --no-audit --no-fund"
  },
  "license": "MIT"
}
